<!--<mvc:View controllerName="fioriapp.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    xmlns:c="sap.ui.core"
>
    <Page id="page" title="{i18n>title}">
        <content>
            <f:SimpleForm  >
                <c:Title text="Simple Form" icon="sap-icon://form"/>
                <f:content>

                    <Label id="FullName" text="FullName"/>
                    <Input id="inputName"  required="true"/>

                    <Label id="field" text="Field"/>
                    <Input id="inputField"  required="true"/>

                    <Label text="Organisation"/>
                    <Select id="select" items="{SelectModel>/}" showSecondaryValues="true" forceSelection="false" required="true">
                        <c:ListItem key="{SelectModel>abb}" text="{SelectModel>abb}" additionalText="{SelectModel>name}"/>
                    </Select>

                    <Button text="Submit" press="onSubmit" type="Accept"/>

                </f:content>

            </f:SimpleForm>

            <Table id="table" headerText="Table" items="{EmployeesModel>/}" mode="Delete" delete="onDelete">
                <columns>
                    <Column>
                        <header>
                        <Label text="Name"/>
                        </header>
                    </Column>
                    <Column>
                        <header>
                        <Label text="Organisation"/>
                        </header>
                    </Column>
                    <Column>
                        <header>
                        <Label text="Field"/>
                        </header>
                    </Column>
                     <Column>
                        <header>
                        <Label text="Actions"/>
                        </header>
                    </Column>
                </columns>
                <ColumnListItem >
                    <cells>
                    <Input value="{EmployeesModel>name}"  editable="{= ${EmployeesModel>isEditable} ? true : false }" />
                    <Input value="{EmployeesModel>org}"   editable="false"/>
                    <Input value="{EmployeesModel>field}" editable="{= ${EmployeesModel>isEditable} ? true : false }" />
                    <Button text="Toggle Editablity" type="Accept" press="onBtnPress"/>
                    </cells>
                </ColumnListItem>
            </Table>
        </content>        
    </Page>


    <SplitApp id="splitAppDummy">
    
    </SplitApp>

</mvc:View>